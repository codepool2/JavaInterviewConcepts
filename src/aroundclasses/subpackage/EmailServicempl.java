package aroundclasses.subpackage;

import aroundclasses.EmailService;


//DO we need to add  import for subpackage classes? yes we should
public class EmailServicempl implements EmailService {
    @Override
    public void sendEMail(EmailDetails emailDetails) {

    }
}
